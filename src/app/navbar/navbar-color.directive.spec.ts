import { NavbarColorDirective } from './navbar-color.directive';

describe('NavbarColorDirective', () => {
  it('should create an instance', () => {
    const directive = new NavbarColorDirective();
    expect(directive).toBeTruthy();
  });
});
